export class NetworkService {
  getHTTPURL() {
    return '';
  }

  parseXMLData() {
    return {};
  }

  formatCSVString() {
    return '';
  }

  // These are fine (allowed acronyms or correct casing)
  getUserById(id: string) { return id; }
  getApiUrl() { return ''; }
  parseHtmlContent() { return ''; }
}
